generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

//creating tables using Prisma with SQLite
//See running npx prisma studio
model User {
  id   String @id @default(uuid())
  name String
}
